#include <locale.h>
#include <stdio.h.>

int buscaBinaria(int num, int vetor);

int main(){
    setlocale(LC_ALL, "Portuguese");

    int vetor = {10,12,16,18,24,30,60,82,94,100};
    int num;

    printf("Digite o numero: ");
    scanf("%i", &num);
    buscaBinaria(num,vetor);

    return(0);
}

int buscaBinaria(int num, int vetor){
    
    int i=0;
    int j=10;

    while(j < 10){
         
    }
}